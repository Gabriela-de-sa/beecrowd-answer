soma = 0
quantidade = 0
for i in range(6):
    valor = float(input())
    if valor > 0:
        soma += valor
        quantidade += 1
media = soma / quantidade
print(f'{quantidade} valores positivos')
print(f'{media:.1f}')
