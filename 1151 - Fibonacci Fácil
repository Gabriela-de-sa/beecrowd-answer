lista = [0,1]

n = int(input())

cont = 2
i = lista[-1]
x = lista[-2]

while cont < n:
    i = lista[-1]
    x = lista[-2]
    sum = i + x # arrumar pois n esta somando
    lista.append(sum)
    cont += 1
print(*lista)
